console.log("JS connected");

$(document).ready(function(){
    console.log('jQuery connected');

    //coundtown function for redirection
/*
    var timeleft = 3;
    var downloadTimer = setInterval(function(){
    if(timeleft <= 0){
        clearInterval(downloadTimer);
        $('.loginTextMid').parent().find('.countdown').text('0');
    } 
    else {
        $('.loginTextMid').parent().find('.countdown').text(timeleft);
    }
    timeleft--;
    }, 1000);
*/

});